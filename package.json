{
  "name": "myapp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prestart": "git pull && npm install --save-exact && cd client/ && npm install --save-exact && npm run build",
    "start": "node ./bin/www"
  },
  "dependencies": {
    "aws-sdk": "^2.446.0",
    "body-parser": "^1.19.0",
    "cookie-parser": "~1.4.3",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "express": "~4.16.0",
    "morgan": "~1.9.0"
  }
}
